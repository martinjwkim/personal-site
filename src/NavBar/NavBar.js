import React from 'react';
import { Box } from 'grommet';

function NavBar() {

  const props = {
    main: {
      position: 'absolute',
    },
  }

  console.log({...props.main})

  return (
    <div>

    </div>
  );
}

export default NavBar;